services:
  postgres:
    image: postgres:17-alpine
    container_name: klinikku-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: klinikku
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
